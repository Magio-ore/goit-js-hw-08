# 🖼️ Галерея зображень з модальним вікном (BasicLightbox)

Цей проект являє собою інтерактивну галерею зображень, створену з використанням чистого JavaScript, HTML та CSS. Для відображення повнорозмірного зображення використовується легковажна та функціональна бібліотека **basicLightbox**.

## ✨ Основний функціонал

* **Динамічне створення розмітки:** Елементи галереї створюються динамічно на основі масиву об'єктів у JavaScript.
* **Делегування подій:** Обробка кліків здійснюється за допомогою делегування на контейнері галереї (`ul.gallery`).
* **Модальне вікно:** При кліку на мініатюру відкривається повнорозмірне зображення у модальному вікні, завдяки бібліотеці `basicLightbox`.
* **Заборона дії за замовчуванням:** Заборонено перехід за посиланням або завантаження зображення при кліку.

## 🛠️ Технології та інструменти

* **HTML5**
* **CSS3** (Flexbox, Grid для сітки галереї)
* **JavaScript (ES6+)**
* **BasicLightbox:** Легка JavaScript-бібліотека для створення модальних вікон.

## 🚀 Як запустити проект

1.  **Клонуйте або завантажте** цей репозиторій.
2.  Відкрийте файл **`index.html`** у будь-якому сучасному веб-браузері.
3.  Переконайтеся, що файли **`style.css`** та **`main.js`** знаходяться в одній директорії з `index.html`.

## 💡 Декомпозиція та реалізація

Проєкт був реалізований поетапно, згідно з принципом декомпозиції:

1.  **HTML-розмітка:** Створено контейнер галереї (`<ul class="gallery">`).
2.  **Дані:** Використано масив об'єктів `images` для даних про зображення.
3.  **Динамічна розмітка:** Створено функцію для генерації елементів галереї (`<li>...</li>`) з використанням методу `map` і вставлено їх у DOM. Посилання на велике зображення збережено в атрибуті `data-source` та `href`.
4.  **Стилі:** Додано стилізацію галереї за допомогою CSS Grid для створення адаптивної сітки.
5.  **Делегування:** Навішано слухач подій на контейнер галереї. Заборонено поведінку за замовчуванням (`event.preventDefault()`).
6.  **BasicLightbox:** Підключено бібліотеку через CDN.
7.  **Модальне вікно:** Реалізовано логіку відкриття модального вікна при кліку на зображення.
8.  **Відображення великого зображення:** Створено розмітку для `basicLightbox` з посиланням на велике зображення, отримане з `data-source` клікнутого елемента.
